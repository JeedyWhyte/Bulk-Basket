package com.bulkbasket.utils

object Constants {
    // For local testing on a physical device, temporarily point this at
    // your PC's local IP address instead (e.g. "http://192.168.x.x:8000/api/v1/").
    // Keep the production URL active for anything that gets built/shared.
    const val BASE_URL = "https://bulkbasket-backend.onrender.com/api/v1/"
}
